I include here a folder that can compile a short source code in Fortran with a makefile and a *make* instruction from a bash shell in Unix (Linux). The compiler in matter
is Intel's Fortran ifx Compiler.
